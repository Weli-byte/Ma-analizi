# 07 — CI

Workflow `.github/workflows/ci.yml`, run 36122384759 on commit `8580908` (branch `remediation/s0-s3`):

| job | result |
|---|---|
| test (Python 3.12) | success |
| test (Python 3.14) | success |
| lock-up-to-date | success |

Steps per test job: checkout → setup-python → `pip install --require-hashes -r requirements.lock` →
`pip install --no-deps -e .` → import smoke test → `ruff check .` → `pytest --cov` (unit, integration, golden,
reproducibility, CLI) → critical-path coverage gate. The earlier run on `c7dc5dd` (old CI, 3.12 only, data tests skipped)
is superseded.

Limits: Linux runners only; Python 3.13 and macOS untested; CI does not run against the real dataset (raw data is
gitignored/licence-restricted).
